
# 无名杀技术栈详细对比 & 架构图解

## 📊 技术栈对比

### 与你熟悉的技术对标

#### 前端部分

| 你知道 | 无名杀中的对应物 | 区别 |
|------|-------------------|------|
| Vue 组件 | UI 元素（div/button 等）| 不是组件化，是面向对象的 DOM 管理 |
| Vuex 状态管理 | `game`、`lib`、`_status` 全局对象 | 更原始，手动管理状态 |
| Vue 事件系统 | `lib.hook` 事件钩子系统 | 类似 EventEmitter，支持游戏特定事件 |
| CSS Scoped | `theme/` 全局样式 + 动态生成 | 没有作用域隔离，需要命名规范 |
| 动态路由 | 游戏模式切换 | 通过改变 `game.state` 实现 |
| 组件库（如 Element） | `ui.create` 工厂函数 | 原生 DOM API 封装 |

#### 后端部分

| 你知道 | 无名杀中的对应物 | 说明 |
|------|-------------------|------|
| Express 路由 | `noname-server.cts` API 路由 | 提供文件操作和 WebSocket API |
| Middleware | Express 中间件 | 处理 CORS、请求解析等 |
| WebSocket 库 | `ws` 库 | 用于多人联机实时通信 |
| RESTful API | HTTP API 端点 | /readFile、/writeFile 等 |
| 数据库 | 本地文件系统 | 配置和存档保存在文件中 |
| 会话管理 | 游戏房间管理 | WebSocket 服务器维护房间状态 |

---

## 🏛️ 整体架构图解

### 三层架构视图

```
┌─────────────────────────────────────────────────────────────────┐
│                          用户浏览器                               │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                      前端应用 (SPA)                          │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │               游戏引擎核心                              │  │ │
│  │  │  ┌──────────────┐  ┌───────────┐  ┌────────────────┐ │  │ │
│  │  │  │   Game       │  │  Library  │  │   _status      │ │  │ │
│  │  │  │   (游戏逻辑) │  │ (游戏库)  │  │  (全局状态)    │ │  │ │
│  │  │  └──────────────┘  └───────────┘  └────────────────┘ │  │ │
│  │  │         ↓                ↓                ↓             │  │ │
│  │  │  ┌──────────────┐  ┌───────────┐  ┌────────────────┐ │  │ │
│  │  │  │     UI       │  │    Get    │  │      AI        │ │  │ │
│  │  │  │  (界面系统)  │  │ (查询接口)│  │  (AI 系统)    │ │  │ │
│  │  │  └──────────────┘  └───────────┘  └────────────────┘ │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  │                         ↓                                    │ │
│  │  ┌────────────────────────────────────────────────────────┐ │ │
│  │  │   样式 + 布局 + 音频 + 资源加载                        │ │ │
│  │  │   theme/  layout/  get.audio  init/                    │ │ │
│  │  └────────────────────────────────────────────────────────┘ │ │
│  │                         ↓                                    │ │
│  │  ┌────────────────────────────────────────────────────────┐ │ │
│  │  │        平台适配层                                      │ │ │
│  │  │  Browser.js / Cordova.js / Node.js                    │ │ │
│  │  └────────────────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────────────┘ │ │
│                                                                   │ │
│  HTTP / WebSocket                                                │ │
│         ↓                                                         │ │
└─────────────────────────────────────────────────────────────────┘
         │
         │ API 请求 / WebSocket 连接
         │
┌────────────────────────────────────────────────────────────────┐
│                      后端服务器 (Node.js)                        │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │            Express 应用 (noname-server.cts)               │ │
│  │  ┌───────────────────────────────────────────────────────┐ │ │
│  │  │  HTTP 路由                                             │ │ │
│  │  │  /readFile        /writeFile        /getFileList      │ │ │
│  │  │  /checkFile       /createDir        /removeDir        │ │ │
│  │  └───────────────────────────────────────────────────────┘ │ │
│  │                                                            │ │
│  │  ┌───────────────────────────────────────────────────────┐ │ │
│  │  │  WebSocket 服务 (scripts/server.js)                   │ │ │
│  │  │  - 房间管理                                           │ │ │
│  │  │  - 玩家同步                                           │ │ │
│  │  │  - 消息广播                                           │ │ │
│  │  └───────────────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────────────┘ │
│                          ↓                                       │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │            文件系统 (配置和存档)                             │ │
│  │  /data/  /config/  /extension/                            │ │
│  └────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
```

---

## 📡 数据流向图

### 单机游戏流程
```
用户操作
  │
  ├─ 选择武将、模式
  │   ↓
  ├─ 初始化 Game 实例
  │   ├─ 创建玩家 (Player)
  │   ├─ 加载武将数据
  │   ├─ 加载卡牌数据
  │   └─ 初始化 UI
  │
  ├─ 游戏循环开始
  │   │
  │   ├─ 当前玩家回合 (Game.phase)
  │   │   ├─ 摸牌 (draw)
  │   │   ├─ 出牌 (play card)
  │   │   │   └─ 触发 'useCard' 事件 → 其他玩家响应
  │   │   └─ 结束回合 (end phase)
  │   │
  │   └─ 下一玩家或 round end
  │       └─ 循环直到游戏结束
  │
  └─ 显示结果
```

### 多人联机流程
```
本地玩家 (主机)              远程玩家 (客机)
      │                           │
      ├─ 初始化游戏               │
      │   └─ 等待其他玩家加入    │
      │                   ┌───────┴─→ 加入房间
      │                           │
      ├─ 开始游戏                 │
      │   ├─ 广播到其他玩家      │
      │   │   ·────────────────────→ 接收游戏初始化
      │   │
      │   ├─ 玩家出牌              │
      │   │                        │
      │   ├─ 广播事件              │
      │   │   ·────────────────────→ 接收事件同步
      │   │                        │
      │   │                    远程玩家响应
      │   │   ┌────────────────────·
      │   │   │ (如使用反杀等)
      │   │
      │   └─ 计算结果，继续游戏   │
      │                           │
      └─ 结束游戏                 └─ 游戏结束
```

### 技能触发流程
```
游戏事件发生 (如 damageStart)
      │
      ├─ 遍历所有玩家
      │   └─ 遍历玩家的所有技能
      │       │
      │       ├─ 技能 trigger 配置包含此事件？
      │       │   └─ Yes → 调用 filter()
      │       │
      │       ├─ filter() 返回 true？
      │       │   └─ Yes → 执行 content()
      │       │
      │       └─ content() 执行完毕
      │           └─ 修改事件对象、显示提示等
      │
      └─ 继续处理下一个事件或回到游戏循环
```

---

## 🔐 数据结构深度解析

### 全局五大对象

```javascript
┌─────────────────────────────────────────────────────────────────┐
│                    全局对象 (window.noname)                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  1️⃣ game (Game 类实例)                                          │
│  ├─ 属性                                                        │
│  │  ├─ players[]          # 玩家列表                           │
│  │  ├─ dead[]             # 已死亡玩家                         │
│  │  ├─ state              # 游戏状态 ('playing', 'over' 等) │
│  │  ├─ phaseNumber        # 回合数                            │
│  │  ├─ roundNumber        # 轮数                              │
│  │  └─ online             # 是否联机                          │
│  │                                                             │
│  └─ 方法                                                        │
│     ├─ start()            # 开始游戏                          │
│     ├─ draw(player)       # 玩家摸牌                          │
│     ├─ getDamage()        # 计算伤害                          │
│     ├─ emit(event)        # 触发事件                          │
│     └─ ...                # 其他游戏逻辑方法                  │
│                                                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  2️⃣ lib (Library 类实例)                                        │
│  ├─ 属性                                                        │
│  │  ├─ element            # 游戏元素工厂                      │
│  │  │   ├─ Player         # 玩家类                           │
│  │  │   ├─ Card           # 卡牌类                           │
│  │  │   ├─ Button          # 按钮类                          │
│  │  │   ├─ Dialog          # 对话框类                        │
│  │  │   └─ ...             # 其他元素                        │
│  │  │                                                         │
│  │  ├─ character           # 武将库                          │
│  │  │   ├─ standard        # 标准版武将                      │
│  │  │   ├─ sp              # SP 版武将                       │
│  │  │   └─ ...             # 其他版本                        │
│  │  │                                                         │
│  │  ├─ card                # 卡牌库                          │
│  │  │   ├─ standard        # 标准卡牌                        │
│  │  │   ├─ sp              # SP 卡牌                         │
│  │  │   └─ ...                                              │
│  │  │                                                         │
│  │  ├─ skill               # 技能库                          │
│  │  ├─ hook                # 事件钩子系统                    │
│  │  ├─ config              # 配置项                          │
│  │  └─ ...                 # 其他库功能                      │
│  │                                                             │
│  └─ 方法                                                        │
│     ├─ skill.add()         # 注册技能                        │
│     ├─ character.add()     # 注册武将                        │
│     ├─ hook.on()           # 注册事件钩子                    │
│     └─ ...                                                    │
│                                                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  3️⃣ ui (UI 类实例)                                              │
│  ├─ 属性                                                        │
│  │  ├─ create              # UI 组件工厂                      │
│  │  │   ├─ card()          # 创建卡牌 DOM                    │
│  │  │   ├─ button()        # 创建按钮 DOM                    │
│  │  │   ├─ player()        # 创建玩家 DOM                    │
│  │  │   ├─ dialog()        # 创建对话框 DOM                  │
│  │  │   └─ ...             # 其他 DOM 元素                   │
│  │  │                                                         │
│  │  ├─ click               # 事件处理器                      │
│  │  │   ├─ card()          # 卡牌点击处理                    │
│  │  │   ├─ button()        # 按钮点击处理                    │
│  │  │   └─ ...                                              │
│  │  │                                                         │
│  │  ├─ dialogs[]           # 所有打开的对话框                │
│  │  ├─ selected            # 选中的卡牌/玩家                 │
│  │  │   ├─ cards[]                                          │
│  │  │   ├─ buttons[]                                        │
│  │  │   └─ targets[]                                        │
│  │  │                                                         │
│  │  └─ arena               # 游戏竞技场 (主 DOM)           │
│  │                                                             │
│  └─ 方法                                                        │
│     ├─ updateLayout()      # 更新布局                        │
│     ├─ updateTimer()       # 更新计时器                      │
│     └─ ...                 # 其他 UI 方法                    │
│                                                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  4️⃣ get (Get 类实例)                                            │
│  ├─ 方法 (查询接口)                                              │
│  │  ├─ playerNumber()      # 获取玩家数量                    │
│  │  ├─ player()            # 查询玩家                        │
│  │  ├─ isPlayer()          # 判断是否玩家                    │
│  │  ├─ cardType()          # 获取卡牌类型                    │
│  │  ├─ itemList()          # 获取物品列表                    │
│  │  ├─ audio()             # 获取音频路径                    │
│  │  └─ ...                 # 其他查询方法                    │
│  │                                                             │
│  └─ 常用查询示例                                                │
│     ├─ get.players()       # 所有玩家                        │
│     ├─ get.player(idx)     # 第 idx 个玩家                   │
│     ├─ get.alive()         # 存活玩家                        │
│     ├─ get.dead()          # 死亡玩家                        │
│     └─ ...                                                    │
│                                                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  5️⃣ ai (AI 类实例)                                              │
│  ├─ 属性                                                        │
│  │  └─ level               # AI 难度等级                      │
│  │                                                             │
│  └─ 方法                                                        │
│     ├─ decide()            # AI 决策                         │
│     ├─ judge()             # AI 评估                         │
│     ├─ chooseCard()        # 选择卡牌                        │
│     └─ ...                 # 其他 AI 方法                    │
│                                                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  _status (全局状态对象，不是类)                                   │
│  ├─ 当前回合信息                                                │
│  │  ├─ currentPhase        # 当前阶段                        │
│  │  ├─ currentPlayer       # 当前玩家                        │
│  │  └─ ...                                                   │
│  │                                                             │
│  ├─ 事件队列                                                    │
│  │  └─ eventQueue[]        # 待处理事件列表                  │
│  │                                                             │
│  ├─ 临时数据                                                    │
│  │  ├─ paused              # 是否暂停                        │
│  │  ├─ activeDialog        # 当前活跃对话框                  │
│  │  └─ ...                 # 其他临时状态                    │
│  │                                                             │
│  └─ 用途：存储游戏进行中不断变化的状态                           │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📝 技能系统的完整生命周期

### 技能注册

```javascript
// 1. 武将定义中声明技能名
character.standard.caocao = {
    name: '曹操',
    skills: ['zhaohu', 'jianxiong']  // ← 声明拥有这两个技能
}

// 2. 在技能库中定义具体实现
lib.skill.zhaohu = {
    name: '招呼',
    description: '...',
    trigger: { global: ['useCard'] },
    filter: (event, source) => { ... },
    content: async (event, trigger, source) => { ... }
}
```

### 技能执行

```
游戏事件 (useCard) 发生
    │
    ├─ 1. 遍历所有玩家
    │  └─ game.players.forEach(player => ...)
    │
    ├─ 2. 遍历玩家的所有技能
    │  └─ player.skills.forEach(skillName => ...)
    │
    ├─ 3. 获取技能定义
    │  └─ lib.skill[skillName]
    │
    ├─ 4. 检查 trigger 配置
    │  └─ if (skill.trigger.global.includes('useCard'))
    │
    ├─ 5. 调用 filter()
    │  └─ if (skill.filter(event, source))
    │
    ├─ 6. 执行 content()
    │  └─ await skill.content(event, trigger, source)
    │
    ├─ 7. 返回结果
    │  └─ event.result = { bool: true, ... }
    │
    └─ 8. 继续处理下一个事件
```

---

## 🎯 关键概念速记表

| 概念 | 定义 | 举例 |
|------|------|------|
| **Player** | 游戏中的一个玩家 | 曹操、孙权等 |
| **Card** | 游戏中的一张卡牌 | 杀、闪、桃等 |
| **Skill** | 武将的特殊能力 | 招呼、简雍等 |
| **GameEvent** | 游戏中发生的事件 | 出牌、受伤、死亡等 |
| **trigger** | 技能何时发动 | global: ['damageStart'] |
| **filter** | 技能是否满足发动条件 | source === this (只有本人发动) |
| **content** | 技能的具体效果 | 减少伤害、抽牌等 |
| **Hook** | 事件钩子，允许扩展修改行为 | lib.hook.on('gameStart', ...) |
| **Prompt** | UI 提示，询问玩家操作 | 等待玩家选择目标 |
| **Dialog** | UI 对话框 | 显示技能效果、确认对话等 |

---

## 🔄 请求响应周期

### HTTP 请求例子
```javascript
// 前端代码
fetch('/readFile', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
        path: 'character/standard.js'
    })
})
.then(res => res.json())
.then(data => {
    console.log('文件内容:', data.content);
});

// 后端响应 (noname-server.cts)
app.post('/readFile', async (req, res) => {
    const { path } = req.body;
    const content = await fs.readFile(path, 'utf-8');
    res.json({
        success: true,
        content: content
    });
});
```

### WebSocket 通信例子
```javascript
// 前端：连接和发送消息
const ws = new WebSocket('ws://localhost:8089');

ws.onopen = () => {
    ws.send(JSON.stringify({
        type: 'playerAction',
        action: 'playCard',
        cardId: '10001',
        targets: ['player2']
    }));
};

ws.onmessage = (event) => {
    const message = JSON.parse(event.data);
    if (message.type === 'gameSync') {
        // 同步远程玩家的游戏状态
        game.players = message.players;
        ui.updateLayout();
    }
};

// 后端：接收和广播
wss.on('connection', (ws) => {
    ws.on('message', (message) => {
        const data = JSON.parse(message);
        // 处理玩家操作
        // 广播到其他玩家
        wss.broadcast(JSON.stringify({
            type: 'gameSync',
            players: game.players
        }));
    });
});
```

---

## 🚀 构建流程详解

```
pnpm build:full --zip
    │
    ├─ 1. 解析参数 (--zip 表示输出压缩包)
    │
    ├─ 2. 配置 Vite 构建参数
    │   ├─ 设置入口: index.html
    │   ├─ 设置输出目录: dist/
    │   └─ 配置插件:
    │       ├─ Vue 编译插件
    │       ├─ 导入映射插件
    │       ├─ JIT 编译插件
    │       └─ 静态文件复制插件
    │
    ├─ 3. Vite 编译前端代码
    │   ├─ 入口: index.html
    │   ├─ 编译: noname/ 所有 .js .ts 文件
    │   ├─ 生成: dist/index.html, dist/noname.js 等
    │   └─ 优化: Tree-shaking, 代码分割等
    │
    ├─ 4. 复制静态资源
    │   ├─ character/          → dist/character/
    │   ├─ card/               → dist/card/
    │   ├─ image/              → dist/image/
    │   ├─ audio/              → dist/audio/
    │   ├─ mode/               → dist/mode/
    │   ├─ layout/             → dist/layout/
    │   ├─ theme/              → dist/theme/
    │   └─ extension/          → dist/extension/
    │
    ├─ 5. 生成导入映射 (importmap)
    │   └─ dist/importmap.json (用于加载第三方库)
    │
    ├─ 6. esbuild 编译后端代码
    │   ├─ 入口: noname-server.cts
    │   ├─ 输出: dist/noname-server.cjs (CommonJS)
    │   └─ 输出: dist/scripts/server.cjs (WebSocket 服务)
    │
    ├─ 7. 输出完整 dist/ 目录
    │   └─ 包含所有源代码 + 所有资源 + 所有扩展
    │
    └─ 8 (可选). 创建 ZIP 压缩包
        └─ dist.zip
```

---

## 💾 文件存储结构

```
项目根目录/
├── src/                        # 源代码
│   ├── noname/                # 核心引擎
│   ├── typings/               # 类型定义
│   └── jit/                   # JIT 编译系统
│
├── public/                    # 静态资源
│   ├── character/             # 武将数据
│   ├── card/                  # 卡牌数据
│   ├── image/                 # 图片资源
│   ├── audio/                 # 音频资源
│   ├── theme/                 # 主题样式
│   ├── layout/                # 布局定义
│   ├── mode/                  # 游戏模式
│   └── extension/             # 扩展
│
├── dist/                      # 构建输出 (build 后生成)
│   ├── index.html             # 入口 HTML
│   ├── noname.js              # 编译后的引擎代码
│   ├── noname-server.cjs      # 后端服务
│   ├── character/             # 所有资源复制到这里
│   ├── image/
│   ├── audio/
│   ├── theme/
│   └── ...
│
├── scripts/                   # 构建脚本
│   ├── build.ts               # Vite 构建配置
│   ├── server.js              # WebSocket 服务器
│   └── ...
│
└── 配置文件
    ├── package.json           # 项目配置
    ├── vite.config.ts         # Vite 配置
    ├── tsconfig.json          # TypeScript 配置
    ├── eslint.config.js       # ESLint 配置
    └── prettier.config.js     # Prettier 配置
```

---

## 🔍 代码查找速查表

| 想查找 | 位置 | 关键词 |
|------|------|--------|
| 武将数据 | character/ | `character.standard.XXX` |
| 卡牌数据 | card/ | `card.standard.XXX` |
| 技能定义 | character/perfectPairs.js | `lib.skill.XXX` |
| UI 组件 | noname/ui/create/ | `ui.create.card()` |
| 游戏流程 | noname/game/index.js | `Game.prototype.` |
| AI 逻辑 | noname/ai/ | `AI.prototype.decide()` |
| 事件系统 | noname/library/hooks/ | `lib.hook.on()` |
| 样式定义 | theme/style/ | `.css` 文件 |
| 布局定义 | layout/default/ | `position`, `margin` |

---

**生成时间：** 2026年1月5日  
**更新版本：** 无名杀 v1.11.0

