
# 无名杀工程结构与技术栈深度分析

## 📋 项目概览

**项目名称：** 无名杀（Noname）  
**项目性质：** 基于 GPL-3.0 的开源游戏引擎  
**项目类型：** 网页游戏/卡牌游戏平台  
**当前版本：** v1.11.0  
**主要用途：** 多人在线卡牌对战游戏（类似三国杀）

---

## 🏗️ 工程整体架构

```
noname/
├── 前端应用层 (Browser)
│   ├── Vue 3 + Vite 构建系统
│   ├── UI 交互层
│   ├── 游戏逻辑层
│   └── AI 系统
│
├── 后端服务层 (Node.js + Express)
│   ├── 开发服务器 (noname-server.cts)
│   ├── WebSocket 联机服务
│   └── 文件系统操作 API
│
├── 跨平台支持
│   ├── Web (浏览器)
│   ├── Cordova (移动端)
│   ├── Electron (桌面端)
│   └── Docker (容器化部署)
│
└── 资源与扩展
    ├── 武将数据 (character/)
    ├── 卡牌数据 (card/)
    ├── 游戏模式 (mode/)
    ├── 音频资源 (audio/)
    ├── 图片资源 (image/)
    ├── 主题皮肤 (theme/)
    └── 扩展系统 (extension/)
```

---

## 🛠️ 技术栈详解

### 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **Vue** | ^3.5.26 | 前端框架，用于 UI 组件和界面交互 |
| **Vite** | ^7.3.0 | 现代化构建工具，支持 HMR（热模块替换）和快速开发 |
| **TypeScript** | ^5.9.3 | 类型安全的 JavaScript 超集 |
| **ESLint** | ^9.39.2 | 代码质量检查工具 |
| **Prettier** | ^3.7.4 | 代码格式化工具 |

### 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **Node.js** | ^20.19.0 or >=22.12.0 | 运行时环境 |
| **Express** | ^5.2.1 | Web 服务框架 |
| **TypeScript** | ^5.9.3 | 类型安全的后端开发 |
| **WebSocket (ws)** | ^8.18.3 | 实时双向通信，支持多人联机 |

### 关键依赖库

| 库 | 版本 | 功能 |
|----|------|------|
| **@codemirror/*** | ^6.x | 代码编辑器（用于扩展编写） |
| **jszip** | ^2.7.0 | 压缩/解压 ZIP 文件 |
| **crypto-js** | ^4.2.0 | 加密库（用于安全通信） |
| **pinyin-pro** | ^3.27.0 | 汉字拼音转换 |
| **stacktrace-js** | ^2.0.2 | 错误堆栈跟踪 |
| **core-js-bundle** | ^3.47.0 | JavaScript polyfill 库 |

### 项目配置工具

- **pnpm** (>=9)：高效的包管理器
- **tsx**：直接运行 TypeScript 文件
- **esbuild**：快速的 JavaScript 打包工具
- **Docker**：容器化部署

---

## 📁 核心目录结构详解

### 1️⃣ 主核心代码 (`noname/`)

#### `noname/entry.js` - 应用入口
```javascript
// 作用：
// - 环境检测（浏览器/Cordova/Electron）
// - GPL 协议确认
// - 启动引导系统
// - 平台相关资源加载
```

#### `noname/ai/` - AI 决策系统
```
ai/
├── index.js          # AI 主类和接口定义
└── basic.js          # 基础 AI 实现（困难模式）
```
**职责：**
- 计算机对手的决策逻辑
- 根据游戏状态评估最优行动
- 支持多个难度等级

#### `noname/game/` - 核心游戏引擎
```
game/
├── index.js              # Game 类，游戏主逻辑
├── check.js              # 游戏规则检查（合法性验证）
├── promises.js           # 异步流程控制
├── PauseManager.ts       # 暂停管理器
└── dynamic-style/        # 动态样式系统
```
**Game 类职责：**
- 维护游戏状态（玩家、回合、卡牌等）
- 管理游戏流程（回合切换、出牌、事件触发）
- 处理网络同步（多人联机）
- 执行游戏逻辑（伤害结算、技能效果等）

#### `noname/ui/` - 用户界面系统
```
ui/
├── index.js           # UI 主类
├── create/            # 组件创建工厂
│   └── index.js       # 创建各类 UI 元素（卡牌、按钮、对话框等）
└── click/             # 事件处理
    └── index.js       # 点击事件逻辑
```
**UI 类职责：**
- 渲染游戏界面
- 处理用户交互（点击、拖拽、长按）
- 管理对话框、菜单等 UI 组件
- 动画和视觉效果

#### `noname/library/` - 游戏库系统（最大文件 ~14000 行）
```
library/
├── index.js              # Library 主类
├── element/              # 游戏元素定义
│   ├── Player.js         # 玩家类
│   ├── Card.js           # 卡牌类
│   ├── Button.js         # 按钮类
│   ├── Dialog.js         # 对话框类
│   ├── GameEvent.js      # 游戏事件
│   └── ...               # 其他元素
├── skill.js              # 技能库
├── announce/             # 公告系统
├── poptip.js             # 提示框管理
├── zhanfa.js             # 战法系统
├── hooks/                # 钩子系统（插件机制）
├── init/                 # 初始化模块
└── update-urls.js        # 更新 URL 配置
```
**Library 类职责：**
- 配置管理（前缀、版本、URLs）
- 游戏元素的中央管理
- 技能系统的注册和调用
- 事件钩子系统（允许扩展拦截和修改游戏行为）

#### `noname/get/` - 游戏数据查询接口
```
get/
├── index.js           # Get 类，提供各类查询方法
├── is.js              # 判断类方法（如 isPlayer、isCard）
├── audio.ts           # 音频资源获取
├── promises.js        # Promise 相关查询
└── pinyins/           # 拼音数据库
```
**Get 类职责：**
- 提供游戏数据查询接口（查询玩家、卡牌、技能等）
- 路径和资源定位
- 配置检查和获取

#### `noname/status/` - 全局状态管理
**_status 对象** 维护：
- 当前游戏进程中的全局状态
- 事件队列
- 暂停状态
- 当前激活的对话框等

#### `noname/util/` - 工具函数库
- 通用工具函数
- 沙箱安全管理
- 配置序列化

#### `noname/init/` - 初始化系统
```
init/
├── index.js          # 启动引导流程
├── browser.js        # 浏览器环境初始化
├── node.js           # Electron 环境初始化
├── cordova.js        # Cordova 环境初始化
├── import.js         # 资源导入系统
├── loading.js        # 加载界面管理
├── polyfill.js       # 兼容性填充
└── onload/           # 加载完成后的初始化
```

---

### 2️⃣ 游戏内容数据 (`character/`, `card/`, `mode/`)

#### `character/` - 武将数据系统
```
character/
├── standard/         # 标准版武将
├── sp/               # SP 版武将
├── sp2/              # SP2 版武将
├── shenhua/          # 神话再临版
├── yingbian/         # 应变版
├── huicui/           # 回萃版
├── diy/              # DIY 武将
├── collab/           # 联动武将
├── offline/          # 离线武将
├── key/              # 键位配置
├── perfectPairs.js   # 完美搭档配置
├── rank.js           # 排名系统
└── replace.js        # 武将替换规则
```
**武将数据结构示例：**
```javascript
{
    // 基础信息
    name: '曹操',           // 武将名
    hp: 8,                  // 初始血量
    sex: 'male',            // 性别
    
    // 技能定义
    skills: ['zhaohu', 'jianxiong'],
    
    // 所属集合
    group: ['qun'],         // 群势力
}
```

#### `card/` - 卡牌集合管理
```
card/
├── standard.js       # 标准卡牌集
├── sp.js             # SP 卡牌集
├── extra.js          # 额外卡牌集
└── ...               # 其他卡牌集
```

#### `mode/` - 游戏模式
```
mode/
├── single.js         # 单机模式
├── versus.js         # 对战模式
├── identity.js       # 身份模式
├── chess.js          # 象棋模式
├── stone.js          # 石牌模式
├── doudizhu.js       # 斗地主模式
└── guozhan/          # 国战模式
```

---

### 3️⃣ UI 与样式系统 (`layout/`, `theme/`, `image/`)

#### `layout/` - 布局系统
```
layout/
├── default/          # 默认布局
├── mobile/           # 移动端布局
├── nova/             # Nova 布局（现代风格）
├── newlayout/        # 新布局
└── others/           # 其他布局
```
**作用：** 定义不同场景下的界面布局方案（玩家位置、卡牌排列、操作面板位置等）

#### `theme/` - 主题系统
```
theme/
├── style/            # CSS 样式
│   ├── layout/       # 布局样式
│   ├── background/   # 背景图片
│   └── ...
├── music/            # 音乐
└── woodden/          # 古风主题
```

#### `image/` - 图片资源
```
image/
├── card/             # 卡牌图片
├── character/        # 武将立绘
├── background/       # 背景图
├── emotion/          # 表情资源
├── mode/             # 模式图标
└── splash/           # 启动画面
```

---

### 4️⃣ 开发与构建系统 (`scripts/`, `jit/`)

#### `scripts/` - 构建脚本
```
scripts/
├── build.ts          # Vite 构建脚本（生成 dist/）
├── server.js         # WebSocket 服务器（联机）
├── server.cjs        # 同上（CommonJS 版）
├── generateImportmap.js     # 导入映射生成
├── generateTestPack.ts      # 测试包生成
└── vite-plugin-importmap.ts # Vite 插件
```

**build.ts 流程：**
1. 读取命令行参数（mode: full/diff/default）
2. 配置静态资源复制列表
3. 使用 Vite 构建（ESM 模块）
4. 使用 esbuild 构建服务端代码
5. 生成 dist/ 目录

#### `jit/` - JIT 编译系统
```
jit/
├── index.ts                 # JIT 入口
├── vite-plugin-jit.ts       # Vite 插件
├── ts-compiler.ts           # TypeScript 编译器
├── compile-strategy.ts      # 编译策略
├── canUse.ts                # 功能检测
└── service-worker.ts        # Service Worker（缓存策略）
```
**作用：** Just-In-Time 编译扩展代码，支持在游戏中实时加载和执行扩展

---

### 5️⃣ 配置文件详解

#### `package.json` - 项目配置
```json
{
    "type": "module",                    // 使用 ESM 模块
    "scripts": {
        "dev": "...",                    // 开发模式（Vite + Express）
        "build": "...",                  // 构建生产版本
        "build:full": "...",             // 构建完整包（含所有资源）
        "build:diff": "...",             // 构建差异包（仅新增资源）
        "lint": "..."                    // 代码检查
    }
}
```

#### `vite.config.ts` - Vite 构建配置
```typescript
{
    root: ".",                          // 项目根目录
    base: "./",                         // 静态资源基础路径
    resolve: {
        alias: {
            "@": "/noname",             // 路径别名
            "noname": "/noname.js"
        }
    },
    server: {
        port: 8080,                     // 前端服务端口
        proxy: {
            "/checkFile": "http://127.0.0.1:8089",   // API 代理
            // ... 其他 API 代理
        }
    }
}
```

#### `tsconfig.json` - TypeScript 配置
```json
{
    "compilerOptions": {
        "target": "ESNext",             // 编译目标
        "module": "ESNext",             // 模块系统
        "moduleResolution": "Bundler",  // 模块解析策略
        "allowJs": true,                // 允许 JS 文件
        "checkJs": true,                // 检查 JS 文件
        "strict": true,                 // 严格模式
        "noImplicitAny": false          // 允许隐式 any
    }
}
```

#### `eslint.config.js` - ESLint 配置
- 检查代码质量
- 支持 Vue 和 TypeScript

#### `prettier.config.js` - 代码格式化
- 统一代码风格

---

### 6️⃣ 后端服务

#### `noname-server.cts` - 开发服务器
```typescript
// 功能：
// - 提供 HTTP API（文件操作）
// - 提供 WebSocket 服务（联机）
// - 支持调试模式

// 暴露的 API 接口：
// POST /checkFile          检查文件存在
// POST /readFile           读取文件（二进制）
// POST /readFileAsText     读取文件（文本）
// POST /writeFile          写入文件
// POST /removeFile         删除文件
// POST /getFileList        获取文件列表
// POST /createDir          创建目录
// POST /removeDir          删除目录
```

#### `scripts/server.js` - WebSocket 服务器
- 处理多人游戏同步
- 房间管理
- 消息广播

---

### 7️⃣ 类型定义系统 (`typings/`)

```
typings/
├── type.d.ts                # 核心类型定义（1000+ 行）
├── Client.d.ts              # Vite 模块声明
├── Skill.d.ts               # 技能类型
├── Card.d.ts                # 卡牌类型
├── Player.d.ts              # 玩家类型
├── Result.d.ts              # 结果类型
├── windowEx.d.ts            # 窗口扩展类型
└── ...                       # 其他类型
```

**关键类型示例：**
```typescript
// 内容函数类型 - 技能的核心定义方式
type ContentFuncByAll = (event: GameEvent, trigger: GameEvent, player: Player) => Promise<any>;

// 生成器内容函数 - 支持分步执行
type GeneratorContentFuncByAll = (event: GameEvent, map: {
    event: GameEvent,
    step: number,
    source: Player,
    player: Player,
    // ...
}) => Generator<any, void, unknown>;

// 类型映射
type SAAType<T> = T | T[];              // Single And Array Type
type SkillOrCard = string | NameType | Card;
```

---

## 🔄 开发工作流与启动流程

### 开发启动流程

```
pnpm dev
    ↓
启动 Express 后端服务 (8089)
启动 Vite 前端开发服务器 (8080)
    ↓
entry.js 执行
    ├── 环境检测
    ├── GPL 确认
    ├── 加载 preload 脚本
    └── 启动 boot() 初始化
        ├── 加载武将数据
        ├── 加载卡牌数据
        ├── 加载游戏模式
        ├── 初始化 UI 系统
        └── 启动游戏主循环
```

### 构建流程

```
pnpm build:full
    ↓
build.ts 执行
    ├── 配置构建参数
    ├── Vite 构建前端代码 → dist/
    │   ├── 编译 noname/ 核心代码
    │   ├── 复制静态资源（character/, card/, etc）
    │   ├── 生成导入映射 (importmap)
    │   └── JIT 编译系统初始化
    │
    ├── esbuild 构建后端代码
    │   └── 生成 dist/noname-server.cjs
    │
    └── 可选：生成 ZIP 压缩包 (--zip)
```

---

## 🎮 游戏执行核心流程

### 游戏初始化流程
```
1. 玩家选择游戏模式
    ↓
2. 初始化 Game 实例
    ├── 创建 Player 对象
    ├── 初始化卡牌池
    ├── 加载技能定义
    └── 初始化 UI
    ↓
3. 游戏开始
    ├── 触发 'gameStart' 事件
    ├── 执行角色"登场"技能
    └── 进入主回合循环
```

### 回合执行循环
```
当前玩家回合开始
    ↓
触发 "phaseStart" 事件（准备阶段）
    ↓
触发 "drawCard" 事件（摸牌）
    ↓
主回合
    ├── 等待玩家操作
    ├── 出牌/使用技能
    │   ├── 触发 "useCard" 事件
    │   ├── 检查目标合法性
    │   ├── 执行卡牌/技能效果
    │   └── 等待响应卡/技能
    │
    └── 重复直到玩家结束回合
    ↓
触发 "phaseEnd" 事件
    ↓
进入下一玩家的回合或结束本轮（roundEnd）
```

### 事件系统
```
游戏事件触发机制：
    ↓
game.emit('eventName', data)
    ↓
Library 中的钩子系统拦截
    ↓
执行技能中的触发条件 (trigger)
    ├── 满足条件？
    │   ├── Yes → 执行技能效果 (content)
    │   └── No → 跳过
    │
└── 返回结果到事件队列
```

---

## 🔌 扩展系统详解

### 扩展位置
```
extension/
├── boss/                # Boss 模式扩展
├── coin/                # 金币系统扩展
├── cardpile/            # 卡牌堆扩展
└── ...                  # 其他社区扩展
```

### 扩展加载机制
1. **扩展发现**：从 extension/ 目录扫描
2. **加载顺序**：按字母序或配置顺序
3. **JIT 编译**：TypeScript 扩展动态编译
4. **沙箱执行**：在 security 管理的上下文中运行
5. **钩子注入**：扩展可以注册事件钩子修改游戏逻辑

### 扩展开发模板
```javascript
export { 
    name: '扩展名',
    description: '扩展描述',
    author: '作者',
    version: '1.0.0',
    
    // 扩展初始化
    onLoad() {
        // 获取全局游戏对象
        const { game, lib, ui, get, ai } = noname;
        
        // 注册武将
        lib.element.Player.extendCharacter({...});
        
        // 注册卡牌
        lib.element.Card.extendCard({...});
        
        // 添加事件钩子
        lib.hook.on('eventName', (data) => {
            // 处理游戏事件
        });
    },
    
    onUnload() {
        // 清理资源
    }
}
```

---

## 🌐 多平台适配

### 平台检测逻辑（entry.js）
```javascript
// 1. 优先检查 Electron
if (typeof window.require === "function") {
    return import("./init/node.js");  // Electron 适配
}

// 2. 检查 Cordova（移动端）
const isCordovaLike = 
    typeof window.cordova !== "undefined" ||
    typeof window.NonameAndroidBridge !== "undefined";

if (isCordovaLike) {
    return import("./init/cordova.js");  // 移动端适配
}

// 3. 默认浏览器
return import("./init/browser.js");      // Web 适配
```

### 各平台资源加载差异
- **Web**：使用 HTTP 请求加载资源
- **Cordova**：使用文件系统 API
- **Electron**：使用 Node.js fs 模块

---

## 📊 核心数据结构

### Player（玩家）
```javascript
{
    name: '玩家名',
    hp: 5,              // 当前血量
    maxHp: 8,           // 最大血量
    sex: 'male',        // 性别
    group: 'shu',       // 阵营
    
    // 卡牌
    cards: [],          // 手牌
    $inventory: [],     // 装备区等
    
    // 状态
    status: {},         // 特殊状态集合
    marks: {},          // 标记集合
    
    // 技能
    skills: [],         // 拥有的技能
    
    // AI
    ai: {...},          // 人工智能决策数据
    
    // 方法示例
    draw(num),          // 摸牌
    damage(num),        // 受伤
    gainMaxHp(num),     // 增加最大血量
    canUse(card),       // 是否可以使用卡牌
}
```

### Card（卡牌）
```javascript
{
    name: '杀',         // 卡牌名
    suit: 'heart',      // 花色（红心、黑桃等）
    number: 5,          // 点数
    type: 'basic',      // 卡牌类型
    subType: '',        // 子类型
    
    // 方法
    use(source, targets),   // 使用卡牌
    respond(source),        // 响应卡
}
```

### GameEvent（游戏事件）
```javascript
{
    name: 'useCard',        // 事件名
    source: player,         // 事件触发者
    target: player,         // 目标玩家
    card: card,             // 相关卡牌
    result: {
        bool: true,         // 事件是否成功
        targets: [],        // 受影响的玩家
    }
}
```

### Skill（技能）
```javascript
{
    name: '技能名',
    
    // 触发条件
    trigger: {
        global: ['useCard'],    // 全局监听的事件
        target: 'useCard',      // 特定类型事件
    },
    filter(event) {             // 触发过滤
        return true;
    },
    
    // 执行逻辑
    async content(event, trigger, player) {
        // 异步执行技能效果
    },
    
    // 配置
    frequent: true,             // 是否是频繁技能
    locked: false,              // 是否锁定
}
```

---

## 🔐 安全与性能

### 沙箱系统
```javascript
// security 对象提供的功能
security.eval(code)            // 安全的代码执行
security.AsyncFunction()        // 安全的异步函数
security.createContext()        // 创建隔离上下文
```

### 性能优化
1. **JIT 编译**：动态加载和编译扩展
2. **Service Worker**：离线缓存和 PWA 支持
3. **代码分割**：按需加载模块
4. **动态样式**：运行时生成样式避免重复

---

## 🚀 开发入门建议

### 第一步：熟悉项目结构（1-2 小时）
1. 阅读本文档
2. 查看 [docs/how-to-start.md](docs/how-to-start.md)
3. 浏览 `noname/` 核心模块

### 第二步：启动开发环境（30 分钟）
```bash
# 1. 安装依赖
pnpm install

# 2. 启动开发服务器
pnpm dev

# 3. 打开浏览器访问 http://localhost:8080
```

### 第三步：理解核心流程（2-3 小时）
1. 在浏览器中打开游戏，观察UI
2. 打开开发者工具 (F12) 查看网络请求
3. 追踪 game、lib、ui 对象的方法调用
4. 查看 [typings/](typings/) 了解类型定义

### 第四步：尝试扩展开发（3-5 小时）
1. 在 `extension/` 创建新的扩展文件夹
2. 参考现有扩展学习 API 调用
3. 注册自定义武将或卡牌
4. 添加事件钩子修改游戏逻辑

### 第五步：深入特定模块（根据需要）

#### 🎨 如果想修改 UI
- 学习：`noname/ui/create/index.js`（组件创建）
- 修改：`layout/` 和 `theme/`（布局和样式）

#### 🤖 如果想改进 AI
- 学习：`noname/ai/`（AI 决策）
- 改进：评估函数和决策树

#### 🎯 如果想添加新的游戏模式
- 参考：`mode/` 中的现有模式
- 实现：Mode 类和事件钩子

#### 🔧 如果想理解技能系统
- 学习：`typings/Skill.d.ts`（技能类型）
- 参考：`character/standard/` 中的武将技能定义

---

## 📖 关键文件快速导航

| 需求 | 对应文件 |
|------|---------|
| 启动流程 | [noname/entry.js](noname/entry.js)、[noname/init/](noname/init/) |
| 游戏逻辑 | [noname/game/index.js](noname/game/index.js) |
| UI 系统 | [noname/ui/](noname/ui/) |
| 技能系统 | [noname/library/skill.js](noname/library/skill.js)、[typings/Skill.d.ts](typings/Skill.d.ts) |
| 武将数据 | [character/](character/) 各版本文件 |
| 构建配置 | [vite.config.ts](vite.config.ts)、[tsconfig.json](tsconfig.json) |
| 类型定义 | [typings/](typings/) |
| 扩展系统 | [extension/](extension/) |
| 部署配置 | [Dockerfile](Dockerfile)、[docker-compose.yml](docker-compose.yml) |

---

## 🆘 常见问题与解答

### Q: 为什么有两个端口 8080 和 8089？
A: 
- **8080**：Vite 前端开发服务器，提供 HMR
- **8089**：Express 后端服务器，提供 API 和 WebSocket

### Q: 如何开发新的武将？
A: 在 `character/` 对应的版本文件中添加武将定义，遵循 Player 数据结构

### Q: 如何添加新的卡牌？
A: 在 `card/` 对应的集合文件中添加卡牌定义

### Q: 扩展如何访问游戏对象？
A: 使用全局 `noname` 对象的导出：`const { game, lib, ui, get, ai } = noname;`

### Q: 如何调试技能效果？
A: 
1. 在技能 content 函数中加入 `console.log`
2. 打开浏览器 DevTools (F12)
3. 在 Console 标签页查看输出

### Q: 如何构建完整的离线包？
A: 
```bash
pnpm build:full --zip
```
这会在项目根目录生成压缩包，包含所有资源

### Q: 多人联机是如何实现的？
A: WebSocket 连接到 Express 服务器，通过事件同步实现实时多人交互

---

## 🤝 贡献指南

1. **Fork** 项目到个人账户
2. **Clone** 到本地开发
3. **创建分支**：`git checkout -b feature/your-feature`
4. **提交更改**：遵循 [PR 规范](https://github.com/libnoname/noname/wiki)
5. **推送到**：PR-Branch 分支
6. **创建 Pull Request**

---

## 📚 推荐学习资源

- [Vue 3 官方文档](https://vuejs.org/)
- [Vite 官方文档](https://vitejs.dev/)
- [TypeScript 官方文档](https://www.typescriptlang.org/)
- [Express 官方文档](https://expressjs.com/)
- [WebSocket 协议](https://tools.ietf.org/html/rfc6455)

---

**最后更新：** 2026年1月5日  
**文档版本：** 1.0（对应无名杀 v1.11.0）

