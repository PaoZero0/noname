
# 无名杀 - 快速上手开发指南

*针对有 Vue 前端基础和 Node.js 后端基础的开发者*

---

## 🎯 30 分钟快速启动

### 1. 环境准备（5 分钟）

```bash
# 确保已安装 Node.js >= 22 和 pnpm >= 9
node -v    # 应该 >= v22.12.0
pnpm -v    # 应该 >= 9

# 进入项目目录
cd c:\Users\Mayn\Documents\GitHub\noname

# 安装依赖
pnpm install
```

### 2. 启动开发服务器（2 分钟）

```bash
# 启动开发环境
# 这会同时启动：
# - Express 后端服务器 (8089)
# - Vite 前端开发服务器 (8080 - 带 HMR)
pnpm dev

# 浏览器应该会自动打开 http://localhost:8080
```

### 3. 查看项目运行（3 分钟）

在浏览器中：
- 选择一个游戏模式（如"单机"）
- 选择武将开始游戏
- 打开开发者工具 (F12) 查看网络请求和控制台输出

---

## 🧠 核心概念理解（需要你的前后端基础）

### 作为 Vue 开发者，你需要了解：

#### ❌ 这里 **没有用** Vue
虽然项目引入了 Vue，但核心游戏逻辑**不是**基于 Vue 组件的。

#### ✅ 这里的 UI 系统是什么？
```javascript
// UI 系统是原生 DOM + 手写事件处理，不是 Vue

// 例子：创建一个卡牌元素
const card = ui.create.card({
    name: '杀',
    suit: 'heart',
    number: 5
});

// UI 自己处理点击事件、拖拽、动画等
ui.click.card(card, () => {
    // 卡牌被点击
});
```

#### 🎨 样式系统
- **不是** Scoped CSS，而是全局样式 + 动态样式
- 样式定义在 `theme/` 文件夹
- 支持多个主题切换
- 运行时动态生成部分样式

#### 📦 数据管理
- **不是** Vuex，而是全局状态对象
- `game`：游戏实例，维护游戏状态
- `lib`：游戏库，提供所有静态数据和工具
- `_status`：全局状态，当前游戏进程的临时状态
- `ui`：UI 系统
- `get`：数据查询接口
- `ai`：AI 系统

---

### 作为 Node.js 开发者，你需要了解：

#### 后端是什么样的？
```typescript
// 后端非常简单，主要是文件操作和 WebSocket

// 1. Express 服务器 (noname-server.cts)
// 提供 HTTP API：
// - 文件读写（配置、存档）
// - 目录管理
// - 调试接口

// 2. WebSocket 服务器 (scripts/server.js)
// 处理多人游戏：
// - 房间管理
// - 消息广播
// - 玩家同步
```

#### 前后端交互
```javascript
// 前端调用后端 API
fetch('/readFile', {
    method: 'POST',
    body: JSON.stringify({ path: '/data/config.json' })
})
.then(res => res.json())
.then(data => {
    // 获取后端的文件内容
});

// WebSocket 通信（多人联机）
const ws = new WebSocket('ws://localhost:8089');
ws.send(JSON.stringify({
    type: 'gameAction',
    action: 'playCard',
    card: cardId
}));
```

---

## 📊 项目的 5 层架构

```
第 5 层：游戏内容
    ├── 武将数据 (character/)
    ├── 卡牌数据 (card/)
    ├── 游戏模式 (mode/)
    └── 扩展系统 (extension/)

第 4 层：游戏逻辑
    ├── 游戏引擎 (game/)
    ├── 技能系统 (lib.element)
    ├── AI 系统 (ai/)
    └── 事件系统 (lib.hook)

第 3 层：用户界面
    ├── UI 组件系统 (ui/create)
    ├── 事件处理 (ui/click)
    ├── 样式系统 (theme/)
    └── 布局系统 (layout/)

第 2 层：基础服务
    ├── 配置管理 (lib.config)
    ├── 音频系统 (get.audio)
    ├── 路径解析 (get.path)
    └── 工具函数 (util/)

第 1 层：平台适配
    ├── Web (browser.js)
    ├── Cordova (cordova.js)
    ├── Electron (node.js)
    └── 后端服务 (Express + WebSocket)
```

---

## 💡 常见开发任务快速指南

### 任务 1️⃣：添加一个新武将

**位置：** `character/standard.js` 或对应版本

```javascript
// 1. 打开文件，找到 export default 的对象
export default {
    // 现有武将...
    
    // 添加新武将
    yangpeng: {
        name: '杨鹏',
        hp: 5,
        sex: 'male',
        group: 'qun',      // 群势力
        skills: ['skilld'],
        
        // 可选：添加武将图片
        picture: 1,
        
        // 可选：添加描述
        description: '这是一个示例武将'
    }
}

// 2. 为武将添加技能（在 lib.element.Player 中注册）
// 详见：character/perfectPairs.js 或对应武将的技能定义
```

**测试方法：**
1. 启动开发服务器
2. 在游戏中选择新武将
3. 打开 DevTools 查看是否有错误

---

### 任务 2️⃣：修改 UI 布局

**位置：** `layout/default/` 或对应布局

```javascript
// layout/default/index.js 定义玩家位置、操作面板等

// 例子：修改玩家位置（对应屏幕上的位置）
export const layout = {
    // 玩家位置（相对坐标）
    position: {
        0: { left: '50%', top: '80%' },      // 自己
        1: { left: '70%', top: '50%' },      // 右边对手
        2: { left: '50%', top: '20%' },      // 上面对手
        3: { left: '30%', top: '50%' },      // 左边对手
    },
    
    // 其他布局参数...
}
```

**测试方法：**
1. 修改后刷新浏览器
2. 观察玩家位置是否变化

---

### 任务 3️⃣：添加一个新技能

**位置：** 在对应武将的技能定义中（通常在 `character/` 中）

```javascript
// 技能定义示例
const skillExample = {
    // 技能基础信息
    name: '示例技能',
    description: '这是一个示例技能',
    
    // 何时触发
    trigger: {
        global: ['damageStart'],  // 监听全局 damageStart 事件
    },
    
    // 是否满足触发条件
    filter(event, source) {
        return source === this;  // 只有本角色才能触发
    },
    
    // 技能效果（异步函数）
    async content(event, trigger, source) {
        // 例子：抵消 1 点伤害
        if (event.damage) {
            event.damage -= 1;
        }
        
        // 提示玩家
        await ui.create.dialog('发动技能！', '当前伤害降低了1');
    },
    
    // 可选：配置
    frequent: false,           // 是否频繁发动
    locked: false,             // 是否可以关闭
};
```

**技能触发流程：**
```
游戏事件发生 → 事件钩子触发
    ↓
检查所有技能的 trigger.global 是否匹配
    ↓
对匹配的技能调用 filter() 检查是否满足条件
    ↓
满足条件？执行 content()
```

---

### 任务 4️⃣：创建一个简单扩展

**位置：** `extension/myExtension/index.js`

```javascript
/**
 * 扩展的导出格式
 */
export { name, description, author, version, onLoad, onUnload };

const name = '我的扩展';
const description = '这是我的第一个扩展';
const author = '作者名';
const version = '1.0.0';

async function onLoad() {
    // 扩展加载时执行
    console.log('扩展已加载');
    
    // 获取游戏对象
    const { game, lib, ui, get, ai } = noname;
    
    // 注册自定义武将
    lib.character['myCharacter'] = {
        yangpeng: {
            name: '杨鹏扩展版',
            hp: 6,
            sex: 'male',
            group: 'qun',
            skills: ['basicSkill']
        }
    };
    
    // 注册事件钩子
    lib.hook.on('gameStart', (data) => {
        console.log('游戏开始了！');
    });
}

async function onUnload() {
    // 扩展卸载时执行
    console.log('扩展已卸载');
}
```

**扩展加载流程：**
1. 游戏启动时扫描 `extension/` 目录
2. 按字母顺序加载每个扩展
3. 调用 `onLoad()` 函数
4. 扩展可以注册武将、卡牌、事件钩子等

---

### 任务 5️⃣：调试游戏逻辑

```javascript
// 在浏览器 DevTools (F12) 的 Console 中直接调用：

// 获取当前游戏对象
const { game, lib, ui, get, ai } = noname;

// 查看当前玩家
console.log(game.players);

// 查看当前玩家的手牌
console.log(game.players[0].cards);

// 查看一个武将的所有技能
console.log(lib.character.standard.sunce.skills);

// 查看全局状态
console.log(_status);

// 手动触发事件
game.emit('damageStart', {
    source: game.players[0],
    target: game.players[1],
    damage: 1
});
```

---

## 🔧 重要概念深入

### 什么是事件系统？

游戏中**一切都是事件**：

```javascript
// 事件流程示例
游戏流程
    ↓
"当前玩家要出杀"
    ↓
触发 'useCard' 事件
    ↓
所有玩家的"反杀"等技能被激活
    ↓
等待响应...
    ↓
伤害结算
    ↓
触发 'damageStart'、'damageEnd' 等事件
    ↓
更新 UI
```

**事件对象结构：**
```javascript
{
    name: 'useCard',           // 事件名
    source: player1,           // 触发者
    target: player2,           // 目标
    card: cardObject,          // 相关卡牌
    result: {                  // 结果
        bool: true,            // 是否成功
        targets: [player2],    // 受影响的玩家
    }
}
```

### 什么是 Filter / Content / Trigger？

这是**技能三要素**：

```javascript
{
    // 1️⃣ TRIGGER - 何时发动
    trigger: { global: ['useCard'] },  // 监听何种事件
    
    // 2️⃣ FILTER - 是否满足条件
    filter(event, source) {            // 过滤条件
        return source === this;        // 只有本角色
    },
    
    // 3️⃣ CONTENT - 做什么效果
    async content(event, trigger, source) {  // 执行效果
        await ui.create.dialog('技能发动！');
        event.damage -= 1;
    }
}
```

**执行流程：**
```
游戏事件发生
    ↓ (1) 检查 trigger
满足监听条件？
    ↓ (2) 检查 filter
满足发动条件？
    ↓ (3) 执行 content
技能效果执行完毕
```

---

## 📂 最常修改的文件

| 任务 | 文件 | 说明 |
|------|------|------|
| 添加武将 | `character/standard.js` | 武将定义 |
| 修改武将技能 | `character/standard.js` | 技能定义通常在同文件或 perfectPairs.js |
| 修改 UI | `layout/default/index.js` | 改变界面布局 |
| 修改样式 | `theme/style/` | 改变颜色、字体等 |
| 添加卡牌 | `card/standard.js` | 卡牌定义 |
| 创建扩展 | `extension/myExt/index.js` | 扩展代码 |
| 修改 AI | `noname/ai/basic.js` | AI 决策 |
| 修改游戏规则 | `noname/game/index.js` | 游戏引擎 |

---

## 🚨 常见错误与解决

### ❌ "模块找不到"错误
```
Error: Cannot find module 'noname'
```
**解决：** 确保在项目根目录运行 `pnpm install`

### ❌ "8080 端口被占用"
```
Port 8080 is already in use
```
**解决：**
```bash
# 方法 1：关闭占用端口的程序
# 方法 2：修改 vite.config.ts 中的 port 配置

# 方法 3：在命令行指定其他端口
pnpm dev -- --port 8081
```

### ❌ "武将找不到"
```
Character 'yangpeng' not found
```
**解决：**
1. 检查 `character/` 中是否定义了武将
2. 检查拼写是否一致
3. 查看浏览器 Console 中的完整错误信息

### ❌ "技能没有执行"
**调试步骤：**
1. 在技能的 `filter()` 中加入 `console.log('filter called')`
2. 在技能的 `content()` 中加入 `console.log('content called')`
3. 打开 DevTools 查看是否调用
4. 检查 `trigger` 配置是否正确

---

## 📚 学习路径建议

### Week 1：理解架构
- Day 1：理解整个项目结构（本文档）
- Day 2：运行项目，在 DevTools 中跟踪代码执行
- Day 3：阅读 `noname/game/index.js` 理解游戏流程
- Day 4：阅读 `noname/library/` 理解库系统
- Day 5：阅读 `noname/ui/` 理解 UI 系统

### Week 2-3：动手实践
- 任务 1：添加新武将
- 任务 2：修改武将技能
- 任务 3：创建扩展
- 任务 4：修改 UI 布局

### Week 4+：深入特定领域
- 🎮 游戏逻辑：研究 `game/check.js` 和技能系统
- 🤖 AI 开发：改进 `ai/basic.js`
- 🎨 UI/UX：优化 `ui/` 和 `theme/`
- 🔌 扩展生态：开发更复杂的扩展

---

## 🎓 核心文件阅读顺序

**第一阶段**（必读，2-3 小时）
1. [noname/entry.js](../noname/entry.js) - 启动入口
2. [noname/init/index.js](../noname/init/index.js) - 初始化流程
3. [noname/game/index.js](../noname/game/index.js) 的前 500 行 - Game 类基础

**第二阶段**（推荐，3-5 小时）
1. [typings/Skill.d.ts](../typings/Skill.d.ts) - 技能类型定义
2. [character/standard.js](../character/standard.js) 的前 100 行 - 武将定义示例
3. [noname/library/element/Player.js](../noname/library/element/) - Player 类

**第三阶段**（高级，5+ 小时）
1. [noname/game/index.js](../noname/game/index.js) 全文 - 完整游戏逻辑
2. [noname/library/element/GameEvent.js](../noname/library/element/) - 事件系统
3. [noname/library/hooks/index.js](../noname/library/hooks/) - 钩子系统

---

## 🔗 快速链接

- 📖 [完整工程分析文档](./ENGINEERING_ANALYSIS.md)
- 🚀 [官方启动指南](./docs/how-to-start.md)
- 🐳 [Docker 部署指南](./guide.md)
- 💬 [GitHub Wiki](https://github.com/libnoname/noname/wiki)
- 📝 [PR 提交规范](https://github.com/libnoname/noname/wiki)

---

**祝你开发愉快！** 🎉

如有任何问题，欢迎查看官方 GitHub Issues 或参考相关文档。

